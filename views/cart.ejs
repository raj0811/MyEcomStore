<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://checkout.razorpay.com/v1/checkout.js"></script>
    <link rel="stylesheet" href="/css/cart.css">
</head>

<body>



    <div class="main">

        <div class="cart-items">
            <h1>Your Cart</h1>
            <% for (let cart of transformedCartItems) {%>
                <div class="cart-bar">
                    <div class="product-img">
                        <img src="/images/products/<%= cart.productImage %>" alt="">
                    </div>
                    <div class="product-data">
                        <h1>
                            <%= cart.productName%>
                        </h1>
                        <h2>
                            <%= cart.productPrice * cart.quantity %>
                        </h2>
                        <h2>
                            <%= cart.quantity %>
                        </h2>
                    </div>
                    <a class="del-btn" href="/products/<%= cart.productId %>/delete"><i class="fa-solid fa-trash"></i></a>
                </div>
                <%}%>

                    <h3>Total Amount: <%= calculateTotalAmount(transformedCartItems) %>
                    </h3>

                    <a class="next" href="/products/confirm?amt=<%= calculateTotalAmount(transformedCartItems) %>">Proceed to Payment</a>
                    
        </div>
        <div class="cart-summary">
            <h1>Cart Summary</h1>
            <div class="cart-summ">
                <h1>Total Amount : Rs <%= calculateTotalAmount(transformedCartItems) %></h1>
                <h2>Total Quantity: <%= calculateTotalQuantity(transformedCartItems) %></h2>
            </div>
        </div>


    </div>




</body>

</html>